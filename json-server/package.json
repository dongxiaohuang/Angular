{
  "name": "json-server",
  "version": "1.0.0",
  "description": "To run: in json-server folder : type:`json-server --watch db.json`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 4321"

  },
  "author": "",
  "license": "ISC"
}
